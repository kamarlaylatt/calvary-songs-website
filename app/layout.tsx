import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Calvary Songs - Christian Hymns & Worship Songs Collection",
  description: "Discover thousands of Christian songs, hymns, praise and worship music in one place. Search, browse, and sing along with your favorite spiritual songs.",
  keywords: "christian songs, hymns, worship songs, praise music, spiritual songs, gospel music, church songs",
  icons: {
    icon: "/favicon.svg",
    apple: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
